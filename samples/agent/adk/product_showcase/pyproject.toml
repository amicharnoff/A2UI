[project]
name = "a2ui-product-showcase"
version = "0.1.0"
description = "A comprehensive A2UI product showcase demo"
readme = "README.md"
requires-python = ">=3.9"

dependencies = [
    "google-genai[adk]>=1.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
